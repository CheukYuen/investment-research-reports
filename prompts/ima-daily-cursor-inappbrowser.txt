在 /Users/leon/Stock/investment-research-reports 执行「Cursor 内置浏览器代发短摘要」。先完整读取 AGENTS.md、docs/ima-daily-summary-runbook.md、config/ima-daily-summary.json 和 .agents/skills/@tencent-adm/ima-skills/SKILL.md 并遵循其指令。真正点击发送和抽取回答时，再按 Runbook「Cursor InAppBrowser 发送要点」做。不开发页面，不调用旧 next/ingest，不提交 Git。

开始前只问两件事，等回答后再看浏览器、再发提问：
1. 指定日期（一天或多天）。没指定就停，不要默认昨天和今天。
2. Cursor 内置浏览器里已经打开的 IMA 页面 URL。

拿到 URL 后：列出已有浏览器标签，按这个 URL 选中那一页，之后只操作这一页。不要新开标签，不要自己输入或跳转地址，不要改到别的知识库或日期目录。对不上这个 URL 就停下来问，不要换页面重试。

然后用程序按 Asia/Shanghai 固定这些日期（跨月跨年要算对）。每个指定日期运行 node scripts/ima-daily-summary.cjs prepare --date YYYYMMDD，成功后再运行 node scripts/ima-manual-prompts.cjs --date YYYYMMDD。复用脚本生成的短摘要 Prompt，不改写、不缩短、不省略标题。批量用 manual_batch_size，默认 25，跳过已有有效摘要。目录不存在或索引失败就说明原因，不用旧索引冒充成功。登录失效、全局限流或资料额度拒绝立即停止。

一次只发一批：把当前最早未完成的一批填进已打开的那一页并发送。多日时先昨天后今天。发送前确认面包屑是目标日期文件夹、输入框是本批完整 Prompt、界面已显示「DS 快速」；不要展开模型菜单。编辑器已是本批就只发一次。下一批另开新对话。其余批次只报日期、批次数和篇数，不把 Prompt 打给用户。指定日期都无需提问时只报已完成。

回答稳定后，抽出真正的「文件名 / 核心摘要」（标题以 .pdf 结尾，摘要以「据该文件」开头），去掉摘要末尾引用编号，写入 manifests/tmp-manual-answer-*.txt，按日期索引核对文件名，用 report-summaries.cjs record 逐篇导入，然后删除临时文件。对不上的和 NO_CONTENT 保持待补，不要编造。导入后再运行 ima-manual-prompts，发下一批，直到指定日期全部完成。

发给 IMA 的内容必须用脚本当前版本：优先用该文件已有 AI 摘要，缺失时补读正文；每篇 2～3 句、约 80～150 字并说明实际来源；保留完整文件名，条目不重复。不要改回强制逐篇读全文的旧 Prompt。

指定日期的摘要都导入后直接收尾：对每个日期运行 node scripts/ima-daily-summary.cjs finalize --date YYYYMMDD；再按 Runbook 第 7 节下载，各日队列按 P0（多日时先昨天后今天）→ P1 → P2 调用 sync-kb-pdfs.cjs download-queue，每次带 --daily-budget 30 --quota-probe-extra 1，共用执行日 30+1 额度。第 31 次停止、额度拒绝、登录失效或全局限流则停止后续下载。最后简报处理量、排序分布、下载数量与额度用量、失败原因和剩余候选。Git 提交等用户明确要求。
