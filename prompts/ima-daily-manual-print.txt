在 /Users/leon/Stock/investment-research-reports 执行“定时打印手动提问词”流程。先完整读取 AGENTS.md、docs/ima-daily-summary-runbook.md、config/ima-daily-summary.json 和 .agents/skills/@tencent-adm/ima-skills/SKILL.md 并遵循其指令。本 prompt 面向任何 AI harness（Kimi、DeepSeek harness、Codex、Claude Code 等），只用通用能力：读文件、运行 node 脚本、写本地文件；不依赖特定 harness 的 skill 注册表或专有工具。此任务只更新索引并打印 Prompt，供用户手动粘贴到 IMA；不开发页面，不控制 Browser/App 问答，不发送提问，不提交 Git。
按 Asia/Shanghai 在运行开始时固定今天和昨天（正确处理跨月跨年），先昨天再今天，所有命令显式传入 --date YYYYMMDD。对每个日期运行 node scripts/ima-daily-summary.cjs prepare --date YYYYMMDD 更新索引；成功后运行 node scripts/ima-manual-prompts.cjs --date YYYYMMDD。复用脚本生成的短摘要 Prompt，不自行重写、缩短或省略标题，不调用旧 next 命令创建自动批次。批量由 manual_batch_size 配置控制，默认25篇；脚本会跳过已有有效摘要并列出全部剩余批次。用程序处理日期和本地文件，避免把完整摘要、HTML或索引反复输出到上下文。
一次只发一批：最终回复只完整打印当前最早未完成的一批 Prompt（先昨天后今天、按批次顺序），单独一个可复制的 text 代码块，代码块外标明日期、目录、批次和篇数；其余待补批次只在代码块外报“日期＋批次数＋总篇数”的排队概况，不打印其 Prompt 内容。不能只给文件链接或说已生成。两天都无需提问时只报已完成；单日目录不存在或索引失败则说明该日原因，不用旧索引冒充更新成功，继续另一天。登录失效、全局限流或资料额度拒绝立即停止。最后用一句话提示用户：在 IMA 对应文件夹对该批提问，把日期和完整回答交回。用户交回回答后，按 Runbook 核对日期索引文件名并通过 report-summaries.cjs record 逐篇导入，然后重新运行 ima-manual-prompts 打印当前最早未完成的下一批，如此循环直到两天全部完成。 打印的 Prompt 必须使用脚本当前版本：优先使用该文件已有AI摘要，缺失时补读正文；每篇2～3句、约80～150字并说明实际来源；保留完整文件名，条目不重复。不要改回强制逐篇读取全文的旧 Prompt。
摘要全部导入后直接收尾，不再等待指令：先对两天分别运行 node scripts/ima-daily-summary.cjs finalize --date YYYYMMDD 完成权威快照与 DeepSeek 正文排序；再按 Runbook 第 7 节统一优先级下载：两天各自队列按 P0（先昨天后今天）→ P1 → P2 顺序调用 sync-kb-pdfs.cjs download-queue，每次带 --daily-budget 30 --quota-probe-extra 1，共用执行日 30+1 额度；任一调用触发第 31 次停止、额度拒绝、登录失效或全局限流即停止全部后续下载。完成后简洁汇报两天处理量、排序分布、下载数量与额度用量、失败原因和剩余候选。Git 提交仍等待用户明确要求。
